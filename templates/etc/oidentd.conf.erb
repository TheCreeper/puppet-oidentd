###
### File managed by Puppet
###

# Configuration for oidentd
# see oidentd.conf(5)
#

default {

	default {

		deny spoof
		deny spoof_all
		deny spoof_privport
		allow random
		allow random_numeric
		allow numeric
		deny hide
	}
}

user "root" {

	default {

		force reply "UNKNOWN"
	}
}